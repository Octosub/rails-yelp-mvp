<div class="m-3">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit "Add", class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
